# @package _global_
# Base Model Configuration

model:
  embedding_dim: 2
  clip_model: "openai/clip-vit-base-patch32"
  freeze_backbone: true
  num_layers: 4
  hidden_dim: 1024
  min_radius: 5
  dropout: 0.1
