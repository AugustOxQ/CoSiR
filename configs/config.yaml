# CoSiR Training Configuration
defaults:
  - dataset: coco
  - model: base
  - train: default
  - experiment: default
  - _self_

# Random seed for reproducibility
seed: 42

# Device configuration
device: cuda

# W&B logging configuration
wandb:
  enabled: true
  project: cosir_phase_1
  entity: augustoxq
  name: ""
  tags: [Local]
  notes: "CoSiR Training"
  mode: online
