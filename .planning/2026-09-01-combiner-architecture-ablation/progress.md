# Progress Log

## 2026-09-01
- User asked (in parallel with another agent monitoring Experiment 16.2's K-scaling training sweep) to define a model *structure* sweep: current combine mechanism, layer count, and buddy dim are all ad hoc, never chosen by ablation or literature.
- Dispatched Codex (`codeagent-wrapper --backend codex`, background, ~15 min) to read the actual model code (`cosirmodel.py`, `combiner.py`, `condition_predictor.py`, `configs/model/*.yaml`) and the buddy-init framing (`docs/superpowers/specs/2026-08-04-buddy-publication-plan-design.md` §1-2), do a literature search, and produce a ranked candidate shortlist grounded in the real code shapes. Read-only except for one output report file.
- Report landed: `docs/reports/2026-09-01_combiner_architecture_brainstorm.md` (7 ranked candidates + axis-priority argument + minimal ablation order + identity-init bug finding).
- Converted the report into a phased task plan: `task_plan.md` (7 phases — brainstorm [done] → plan [done] → implement 3 new combiner classes + unit tests [code-only, unblocked] → smoke sweep [GPU-gated on Experiment 16.2 finishing] → buddy_dim sweep → rank/depth sweep → delivery).
- Checked GPU state (`nvidia-smi`): single local RTX 3090, ~3.3GB/24GB in use by the Experiment 16.2 process — confirms Phase 4 (training) must wait, but Phase 3 (implementation, no GPU) is unblocked now.
- Did not start implementation this session — plan was the requested deliverable; awaiting user go-ahead to dispatch Phase 3 to Codex.

## 2026-09-02 — Phase 3 implemented
- Added identity-initialized `CombinerResidualControl`, `CombinerLowRankAdapter`, and `CombinerFiLMResidual`, plus `CoSiRModel.combiner_type` selection (`residual_control`, `lowrank`, `film`) while retaining `Combiner_new` for `legacy` and all other values.
- Added the default `combiner_type: "legacy"` model config and passed it through the training model construction.
- Focused identity-init test: `conda activate CoSiR && python src/test/20260902_combiner_architecture/test_identity_init.py` — passed: `Phase 3 combiner identity-init checks passed.`
- Note: first Codex attempt at Phase 3 stalled in its own internal "obtain dual-model analysis" step (`.ccg/tasks/combiner-architecture-phase-3/`, stuck in `currentPhase: "analysis"`) and produced only the test file, no implementation, before being interrupted by a session restart. Re-dispatched with an explicit instruction to skip that step and implement directly against the existing test file's contract; this attempt completed cleanly.
- Code review (Claude, independent — re-ran the test myself rather than trusting the report) caught one real gap: `CombinerFiLMResidual`'s gamma was unbounded, contradicting the brainstorm report's explicit recommendation (candidate 2) to bound gamma with a tanh so the multiplicative gate can't grow unboundedly and destabilize the frozen CLIP geometry. Fixed directly (not re-delegated, small well-specified change): `gamma = gamma_scale * tanh(gamma_raw)` applied after the existing zero-init projection, `gamma_scale=0.1` default, new constructor arg for the eventual ablation. Re-verified test still passes. See `.claude/20260902_log.md` for the diff.

## 2026-09-02 — Phase 4 smoke sweep run
- Wrote `scripts/run_combiner_architecture_smoke.sh` (bash loop over `model.combiner_type`, fixed K=30/alpha=0.5/buddy_dim=16 operating point, `redcaps_150k`, 30 epochs) and `scripts/analyze_combiner_architecture_smoke.py` (modeled on `analyze_buddy_k_ablation.py`) directly rather than delegating to Codex, given it had stalled twice this session on smaller Phase 3 tasks.
- Hit one bug in the first draft: copied `${WANDB_TAG:+++wandb.tags=...}` from the K-ablation script but wrote a literal `+++` (should be `++`) since this script always sets its own tag unconditionally — Hydra rejected the override grammar. Fixed before the SMOKE=1 check.
- `SMOKE=1 bash scripts/run_combiner_architecture_smoke.sh` (2 epochs, `lowrank`) passed end-to-end before committing to the full sweep.
- Launched the full 4-arm sweep (legacy, residual_control, lowrank, film), single seed each, ~21 min total; verified via `nvidia-smi` beforehand that the GPU was idle (Experiment 16.2 had already finished). All 4 completed (`Training Complete!`), 0 failure signatures in the log.
- Analyzer initially reported 5 finished runs for 4 combiner_types: the earlier `SMOKE=1` pipeline-sanity run for `lowrank` shared the same group/tag/seed as the real run, and `.max()`-per-seed silently blended a 2-epoch run's numbers into the 30-epoch cell, distorting `lowrank`'s `test_pre_diff` result specifically. Caught by checking `wandb` run `created_at`/`epoch` directly rather than trusting the first analyzer pass. Fixed the analyzer to keep only the max-epoch run per (combiner_type, seed); re-ran, `lowrank`'s `test_pre_diff` numbers changed from the contaminated -2.30/-4.20 to the correct -5.00/-7.20.
- Results (single seed, directional): `lowrank` wins oracle i2t R1 (+3.0 vs legacy) with the smallest `test_pre_diff` regression among the three new families (-3.4/-3.6 vs legacy). `residual_control` and `film` both show a large `test_pre_diff` regression (-14 to -28) that looks real rather than pure single-seed noise. Full table in `findings.md`.
- Did not instrument residual-norm/cosine-shift/gate-range logging (plan's Phase 4 diagnostic wishlist) — no existing hook for it; flagged as a gap rather than silently skipped.
- Left as an open decision for the user: add 2 more seeds to confirm the regression before ruling out `residual_control`/`film`, or proceed straight to the Phase 5 `buddy_dim` sweep on `lowrank` alone.
