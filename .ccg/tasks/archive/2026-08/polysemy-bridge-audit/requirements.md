# Task 1 Requirements

Promote the existing `classify_edges` and `bridge_node_stats` helpers from the
one-off disagreement diagnostic into `src/conditional_buddy/buddy_graph.py`.
Preserve the existing behavior and diagnostic-module re-exports, add the
specified regression test, record the Task 1 log entry, and commit only the
four paths named by Task 1 Step 7.
